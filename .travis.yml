dist: trusty
language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  
addons:
  sonarcloud:
    organization: "allisondlucca-github"
    token: 
      secure: ${SONAR_TOKEN}

install:
  - pip install codecov

script:
  - coverage run --omit=*test* -m unittest
  - sonar-scanner

after_success:
  - codecov
